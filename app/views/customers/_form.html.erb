<%= form_for(customer) do |f| %>
  <% if customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :division %>
    <%= f.text_area :division %>
  </div>

  <div class="field">
    <%= f.label :site %>
    <%= f.text_area :site %>
  </div>

  <div class="field">
    <%= f.label :mailing_adress %>
    <%= f.text_area :mailing_adress %>
  </div>

  <div class="field">
    <%= f.label :kms_version_id %>
    <%= f.text_field :kms_version_id %>
  </div>

  <div class="field">
    <%= f.label :project_code %>
    <%= f.text_field :project_code %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
